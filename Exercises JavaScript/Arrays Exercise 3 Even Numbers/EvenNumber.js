//Write a JavaScript program that takes an array of numbers and prints all the even numbers from the array.

function Demo(input) {
  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 === 0) {
      console.log(numbers[i]);
    }
  }
}
Demo(1,2,3,4,5,6);