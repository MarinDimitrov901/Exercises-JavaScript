//Create a JavaScript program using nested loops to generate the multiplication table for numbers 1 to 5. Print the table in a readable format.

 function demo(input) {
  
let start = 1;
let end = 5;

for (let i = start; i <= end; i++) {
  console.log(`${start} * ${end} = ${start*end}`);

  }
}
 demo();